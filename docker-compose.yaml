version: '3.8'
services:
  back:
    build: .
    ports:
      - '3000:3000'
    depends_on:
      - redis
      - db
  redis:
    image: redis
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: test1234321
